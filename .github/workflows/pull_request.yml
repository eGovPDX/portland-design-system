name: Test pull request

on:
  pull_request:

jobs:
  test-builds:
    runs-on: ubuntu-latest
    steps:
      - uses: ./packages/actions/setup-build

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: turbo build
