import { resolve } from "path";

export const SOURCE_DIR = resolve("src");
export const ASSETS_DIR = resolve(SOURCE_DIR, "assets");
export const TOKENS_DIR = resolve(SOURCE_DIR, "tokens");
export const DIST_DIR = resolve("dist");
