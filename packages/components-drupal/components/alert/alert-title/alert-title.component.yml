$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

name: Alert Title
status: experimental

props:
  type: object
  properties:
    as:
      type: string
      title: HTML element type
      description: The title wrapper element. Use a heading element when the page context requires one.
      enum:
        - header
        - h2
        - h3
        - h4
        - h5
        - h6
      default: header
    attributes:
      type: object
      title: Additional attributes

slots:
  alert_title_content:
    title: Alert title content
