# This is so your IDE knows about the syntax for fixes and autocomplete.
$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

# The human readable name.
name: Box

# A longer description.
description: A native Twig Box component that renders a simple colored box. Server-side rendered implementation matching the Portland Design System box styles.

# Status can be: "experimental", "stable", "deprecated", "obsolete".
status: experimental

# Schema for the props. We support www.json-schema.org. Learn more about the syntax there.
props:
  # Props are always an object with keys. Each key is a variable in your component template.
  type: object

  properties:
    as:
      type: string
      title: HTML element type
      description: The HTML element to render the box as (e.g., div, section, article)
      enum:
        - div
        - section
        - article
        - aside
        - main
        - header
        - footer
        - nav
      examples:
        - div

    color:
      type: string
      title: Box color
      description: Color scheme for background and content colors
      enum:
        - default
        - primary
        - secondary
        - success
        - danger
        - warning
        - info
        - disabled
        - fixed
      examples:
        - default

    variant:
      type: string
      title: Box variant
      description: Color variation within the chosen color scheme
      enum:
        - subtle
        - moderate
        - emphasis
        - strong
        - light
        - dark
      examples:
        - moderate

slots:
  box_content:
    title: Content
    description: The main content/text of the box.

libraryOverrides:
  dependencies:
    - core/drupal
  js:
    box.js: { attributes: { type: "module" } }
