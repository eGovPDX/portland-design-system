{% set element = as|default('section') %}

{% set classes = ["card__body"] %}

{% embed "portland:box" with {
	as: element,
	attributes: attributes.addClass(classes)
}%}
  {% block box_content %}
    {{ card_body_content }}
  {% endblock %}
{% endembed %}
